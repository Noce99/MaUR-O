build/
build*/
out/
.cache/
compile_commands.json



target/

# Where the `benchmark` tool puts one folder per run.
Results/

# Benchmark suites: the maps are not ours to distribute.
OMOLOGATE
benchmark*.zip
examples

# Where the `create_benchmark` tool puts the archives it builds.
benchmarks/

# Where the `generate_maps_dataset` tool puts the maps it generates.
dataset/

a_lot_of_work

# Where the `train` tool puts one folder per run.
trainings/

# Machine-local cargo settings (e.g. the CUDA toolkit version this machine's
# cudarc build needs) -- not something to push, since it is a fact about this
# machine rather than about the project.
.cargo/


batches
